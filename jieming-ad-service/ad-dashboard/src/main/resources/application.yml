server:
  port: 7002

eureka:
  client:
    service-url:
      defaultZone: http://server1:8000/eureka/

#暴露全部的监控信息
management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: ad-dashboard
